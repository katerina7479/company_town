version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - gocritic
    - gofmt
    - goimports
    - misspell
    - unconvert
    - unparam
  settings:
    gosec:
      excludes:
        - G104  # Unhandled errors (too noisy for now)

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
