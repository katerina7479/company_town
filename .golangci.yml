version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - gosec
    - gocritic
    - misspell
    - unconvert
    - unparam
  settings:
    gosec:
      excludes:
        - G104  # Unhandled errors (too noisy for now)

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
